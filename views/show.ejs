<!DOCTYPE html>
<html>
    <head>
        <meta charset="utf-8">
         <% include ./partials/head.ejs %>
        <title></title>
    </head>
    <body>
    <div class="container">
    <% include ./partials/header.ejs %>
    <a href="/dockit"><i class="fas fa-angle-double-left"></i></a>
            <div class="row">
            <div class="three columns">
            <span>distance</span> 
             <h4><%=entry.distance%></h4>
             <p>kilometes</p> 
             <span>zone</span>
            <h5><%=entry.zone%></h5></a>
            </div>
            <div class="nine columns">
            <span>description</span>
            <p class="detail_description"><%=entry.description%></p>
            </div>
            <form action="/dockit/<%= entry._id %>?_method=DELETE" method="POST">
            <input type="submit" value="DELETE"/>
            <a href="/dockit/<%=entry._id%>/edit"><i class="fas fa-pen-square"></i></a>
            </form>

     </div>         
    </body>
</html>